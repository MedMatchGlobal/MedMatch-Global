name: Weekly UK Drug List Update

on:
  schedule:
    - cron: '0 0 * * 0'  # Every Sunday at 00:00 UTC
  workflow_dispatch:

jobs:
  update-uk-drugs:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm ci

      - name: Run drug extraction script
        run: node scripts/extract-uk-drugs.js

      - name: Check if uk-drugs.json has changed
        id: check_changes
        run: |
          git fetch origin main
          if git diff --quiet origin/main -- public/data/uk-drugs.json; then
            echo "changed=false" >> $GITHUB_ENV
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_ENV
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit updated uk-drugs.json
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/data/uk-drugs.json
          git commit -m "🔄 Weekly UK drug list update"
          git push






